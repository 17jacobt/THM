To search for networks you can turn your network card in monitor mode by the command: sudo airodump-ng <network interface>
Once you find a network back out and use the command: sudo airodump-ng <network interface> --bssid <AP>
